Prompt Archive Status

This file previously stored repeated prompt dumps from an older prompt architecture that relied on a separate global completeness tier.

That model is no longer current.

Current source of truth:
- Global refinement prompt: `query_refinement_module/schema/templates/global_system.py`
- Open-LLM global refinement prompt: `query_refinement_module/schema/templates/global_system_open_llm.py`
- Dimension prompt: `query_refinement_module/schema/templates/dimension.py`
- User-context prompt: `query_refinement_module/schema/templates/user_context.py`
- Open-LLM user-context prompt: `query_refinement_module/schema/templates/user_context_open_llm.py`

Current completeness model:
- Completeness is defined directly by each dimension's `specifications` text.
- Use `Required`, `Required if applicable`, and optional specificity guidance inside the dimension.
- Extract valid anchors first, then ask only for remaining required detail.
- Do not rely on retired global completeness tiers.

If you need a stable snapshot for review, regenerate it from the live templates and the active framework definitions instead of using the retired archive content.