services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: query-refinement-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-query_refinement}
      POSTGRES_USER: ${POSTGRES_USER:-qruser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    volumes:
      - ${DATA_ROOT:-./data}/postgres:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_BIND_ADDRESS:-127.0.0.1}:${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-qruser} -d ${POSTGRES_DB:-query_refinement}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - query-refinement-net

  # Redis for sessions and rate limiting
  redis:
    image: redis:7-alpine
    container_name: query-refinement-redis
    command: redis-server --appendonly yes
    volumes:
      - ${DATA_ROOT:-./data}/redis:/data
    ports:
      - "${REDIS_BIND_ADDRESS:-127.0.0.1}:${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - query-refinement-net

  # API
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: query-refinement-api
    env_file:
      - .env
    environment:
      PATH: /app/.venv/bin:/usr/local/bin:/usr/bin:/bin
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-qruser}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-query_refinement}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "8001:8001"
    volumes:
      - ./refinement_frameworks:/app/refinement_frameworks:ro
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "python3", "-c", "import requests; requests.get('http://localhost:8001/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    command: >
      sh -c "
        echo 'Waiting for database...' &&
        sleep 5 &&
        echo 'Running database migrations...' &&
        python3 -m alembic upgrade head &&
        echo 'Starting application...' &&
        python3 -m gunicorn -c gunicorn_conf.py query_refinement_module.api.main:app
      "
    networks:
      query-refinement-net:
        aliases:
          - api
    restart: unless-stopped

  # Chainlit web UI
  chainlit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: query-refinement-chainlit
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-qruser}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-query_refinement}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
    ports:
      - "${CHAINLIT_PORT:-8501}:8501"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./refinement_frameworks:/app/refinement_frameworks:ro
      - ./logs:/app/logs
    command: >
      sh -c "
        echo 'Waiting for database...' &&
        sleep 5 &&
        echo 'Starting Chainlit...' &&
        python3 -m chainlit run query_refinement_module/chainlit_app.py --host 0.0.0.0 --port 8501
      "
    healthcheck:
      test: ["CMD", "python3", "-c", "import requests; requests.get('http://localhost:8501', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - query-refinement-net
    restart: unless-stopped

networks:
  query-refinement-net:
    driver: bridge
